# 修复和完善右键菜单功能

## 问题分析
根据当前代码分析，右键菜单功能存在以下问题：

1. **备份节点的右键菜单缺失**：当前代码中没有实现备份节点的右键菜单功能
2. **视图节点右键菜单功能不完整**：缺少新建视图、打开视图、设计视图、导出向导等选项
3. **其他节点右键菜单功能需要完善**：函数、事件、用户等节点的右键菜单功能不够丰富
4. **工具栏按钮与右键菜单功能需要保持一致**

## 解决方案

### 1. 完善现有右键菜单功能
- **表节点右键菜单**：保持现有功能，确保包含所有必要选项
- **视图节点右键菜单**：添加新建视图、打开视图、设计视图、导出向导等选项
- **函数节点右键菜单**：完善函数节点的右键菜单功能
- **事件节点右键菜单**：完善事件节点的右键菜单功能
- **用户节点右键菜单**：完善用户节点的右键菜单功能

### 2. 新增备份节点右键菜单
- 实现备份节点的右键菜单，包含新建备份、还原备份、提取SQL等选项

### 3. 确保工具栏与右键菜单功能一致
- 确保顶部工具栏的按钮功能与右键菜单的功能一致
- 为工具栏按钮添加合适的图标，使其与Navicat的样式相似

### 4. 实现缺失的方法
- 对于右键菜单中引用但尚未实现的方法，需要实现相应的功能

## 实现步骤

1. **修改on_db_object_context_menu方法**：
   - 完善视图节点的右键菜单，添加新建视图、打开视图、设计视图、导出向导等选项
   - 添加备份节点的右键菜单处理
   - 完善其他节点的右键菜单功能

2. **实现缺失的方法**：
   - 实现新建视图、打开视图、设计视图等方法
   - 实现备份相关的方法，如新建备份、还原备份、提取SQL等

3. **测试功能**：
   - 确保所有右键菜单功能都能正常工作
   - 确保工具栏按钮与右键菜单功能一致

4. **优化用户体验**：
   - 确保右键菜单的样式和功能与Navicat一致
   - 提供良好的用户体验

## 预期效果

- 数据库对象树的右键菜单功能与Navicat一致
- 备份节点的右键菜单功能完整
- 工具栏按钮与右键菜单功能一致
- 用户可以通过右键菜单方便地执行各种数据库操作